# NBEmail 外部SMTP配置示例
# 复制此文件为 .env 并修改相应配置

# Web服务配置
WEB_PORT=8080
SMTP_PORT=25
DB_PATH=nbemail.db
DOMAIN=localhost

# 管理员配置
ADMIN_EMAIL=admin@localhost
ADMIN_PASS=admin123
JWT_SECRET=nbemail-secret-key-2024

# 外部SMTP发送配置（用于发送邮件到外部邮箱）
# 如果不配置这些参数，系统只能在本地用户之间发送邮件

# ===== 推荐：使用自己域名的SMTP服务器 =====
# 这样可以直接使用您域名的邮箱作为发件人

# 自己域名的SMTP服务器配置
# OUTBOUND_SMTP_HOST=mail.yourdomain.com
# OUTBOUND_SMTP_PORT=587
# OUTBOUND_SMTP_USER=smtp-user@yourdomain.com
# OUTBOUND_SMTP_PASSWORD=your-smtp-password
# OUTBOUND_SMTP_TLS=true

# 如果您的SMTP服务器不需要认证（内网环境），可以不设置用户名和密码
# OUTBOUND_SMTP_HOST=mail.yourdomain.com
# OUTBOUND_SMTP_PORT=25
# OUTBOUND_SMTP_USER=
# OUTBOUND_SMTP_PASSWORD=
# OUTBOUND_SMTP_TLS=false

# ===== 第三方邮件服务商SMTP配置示例 =====
# 注意：使用第三方SMTP时，发件人地址会被限制为认证账号

# Gmail SMTP配置示例
# OUTBOUND_SMTP_HOST=smtp.gmail.com
# OUTBOUND_SMTP_PORT=587
# OUTBOUND_SMTP_USER=your-email@gmail.com
# OUTBOUND_SMTP_PASSWORD=your-app-password
# OUTBOUND_SMTP_TLS=true

# QQ邮箱SMTP配置示例
# OUTBOUND_SMTP_HOST=smtp.qq.com
# OUTBOUND_SMTP_PORT=587
# OUTBOUND_SMTP_USER=your-email@qq.com
# OUTBOUND_SMTP_PASSWORD=your-authorization-code
# OUTBOUND_SMTP_TLS=true

# 163邮箱SMTP配置示例
# OUTBOUND_SMTP_HOST=smtp.163.com
# OUTBOUND_SMTP_PORT=587
# OUTBOUND_SMTP_USER=your-email@163.com
# OUTBOUND_SMTP_PASSWORD=your-authorization-code
# OUTBOUND_SMTP_TLS=true

# Outlook/Hotmail SMTP配置示例
# OUTBOUND_SMTP_HOST=smtp-mail.outlook.com
# OUTBOUND_SMTP_PORT=587
# OUTBOUND_SMTP_USER=your-email@outlook.com
# OUTBOUND_SMTP_PASSWORD=your-password
# OUTBOUND_SMTP_TLS=true

# 企业邮箱SMTP配置示例
# OUTBOUND_SMTP_HOST=smtp.your-company.com
# OUTBOUND_SMTP_PORT=587
# OUTBOUND_SMTP_USER=your-email@your-company.com
# OUTBOUND_SMTP_PASSWORD=your-password
# OUTBOUND_SMTP_TLS=true

# 注意事项：
# 1. 推荐使用自己域名的SMTP服务器，这样可以使用任意域名邮箱作为发件人
# 2. 对于第三方邮件服务商：
#    - Gmail需要使用应用专用密码，不是普通密码
#    - QQ邮箱和163邮箱需要使用授权码，不是登录密码
#    - 发件人地址通常会被限制为认证账号
# 3. 大部分现代邮件服务商都要求使用TLS加密
# 4. 端口25通常用于服务器间通信，587用于客户端提交
